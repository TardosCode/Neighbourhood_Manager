This folder holds your global app settings (audio, theme, video).

Files:
  audio.json   - music & sfx volume, on/off
  theme.json   - custom color overrides
  video.json   - fullscreen toggle and other display options

These files are created automatically when you change a setting. They
apply across all profiles and clans.
