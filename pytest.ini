[pytest]
# The app keeps its source in src/ and uses flat module imports
# (e.g. `import neighborhood_manager`). Putting src/ on the import path lets
# the pure-logic tests import those modules directly. Tests live in tests/.
pythonpath = src
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
