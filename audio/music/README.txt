Drop .mp3 files in this folder to use them as background music.

The program will:
  - read the folder once at startup and randomize the order
  - play tracks one after another, looping the shuffled list
  - re-shuffle when the list reaches the end

If you add new files while the program is running, click "Reload music"
in App Settings -> Audio to pick them up.
