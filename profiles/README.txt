This folder will hold your profile JSON files (one per profile, plus an
_active.json that remembers which profile was last used).

Files here are created automatically when you create profiles in the app.
You can back up this folder or copy it between machines.
