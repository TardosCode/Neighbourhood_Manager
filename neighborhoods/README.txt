This folder will hold your neighborhood (clan) database files.

Each clan is stored as a single JSON file containing the entire member
roster, snapshots, and metadata. An _active.json file remembers which
clan was last selected.

Files here are created automatically when you create a neighborhood in
the app. You can back up this folder or copy it between machines to
share data, just like the profiles folder.
